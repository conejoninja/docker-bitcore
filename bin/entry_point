#!/bin/bash

set -ex

# Generate bitcoin.conf
docker_config

if [ ! -d /bitcoin/.bitcoin/blocks ]; then
    # Download via bittorrent file, fail quietly
   blockchain_bootstrap || true
fi


exec bitcoind &
sleep 20
cd /opt/bitcore-node/ && NODE_ENV=docker npm start